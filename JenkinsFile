pipeline {
    agent any

    stages {
           stage('Checkout') {
                 steps {
                       git branch: 'main',
                             url: 'https://github.com/bkuptanusree-netizen/demo.git'
                }
           }

         stage('Build') {
               steps  {
                     bat 'mvn clean package'
               }
          }
      }
}